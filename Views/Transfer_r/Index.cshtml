@model IEnumerable<edccAdvisingProject.Models.Transfer_r>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.edcc_program.ProgCode)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.course.EdccCode)
        </th>
     
        <th>
            @Html.DisplayNameFor(model => model.major.Major1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.University.University1)
        <th>
            @Html.DisplayNameFor(model => model.Credit)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.edcc_program.ProgCode)
            </td>
        
            <td>
                @Html.DisplayFor(modelItem => item.course.EdccCode)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.major.Major1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.University.University1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Credit)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TR_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.TR_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TR_ID })
            </td>
        </tr>
    }

</table>
